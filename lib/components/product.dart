class Product {
  //Clase en forma de componente donde se definen las imágenes utilizadas en varias secciones de la app
  final String imagePath;

  final String title;

  final double cost;

  final int reviewCount;

  Product(this.imagePath, this.title, this.cost, this.reviewCount);
}
